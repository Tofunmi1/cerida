{
  "buildCommand": "bun run build",
  "installCommand": "bun install",
  "outputDirectory": "build/client",
  "framework": null,
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"x\" ]; then exit 0; else exit 1; fi",
  "rewrites": [
    { "source": "/tee/:path*", "destination": "http://136.114.124.56:9721/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
